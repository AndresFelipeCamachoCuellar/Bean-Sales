# CLAUDE.md — Contexto del proyecto (leer al iniciar cada sesión)

> Este archivo es la memoria del proyecto. Actualízalo cuando cambien decisiones clave.
> Rol de Claude: **Project Manager**. Andrés es el **Product Owner**. Preguntar como PO antes de asumir; administrar TODAS las áreas (código, legal, operaciones, finanzas, GTM), no solo código.
> Idioma de trabajo: español.

---

## 1. Qué es el proyecto

**The Kng Bean / Coffee Global Hub** — Hub **B2B2C de consolidación y exportación de café de especialidad colombiano**.
Compra/consigna café a marcas y fincas locales, lo centraliza en una **bodega en Colombia**, y lo vende a consumidores y pequeños negocios en el exterior (EE.UU., Canadá, Europa) con compra unificada ("el supermercado global del café colombiano").

**Modelo de monetización híbrido:**
- **Compra directa (retail arbitrage):** Bean compra el lote por adelantado y revende. Margen 30-60%. Bean asume flete a bodega.
- **Consignación (incubadora):** para marcas nuevas, el proveedor mantiene propiedad hasta que se vende y paga comisión 20-30%. El proveedor asume el flete a bodega.

Bean actúa como **Merchant of Record / exportador legal**: el proveedor local no necesita licencia de exportación.

---

## 2. Historia importante (evita reabrir debates ya cerrados)

El proyecto **pivoteó dos veces**. Hubo tres modelos conviviendo:
1. **Subastas** (Trello jun 2025) — DESCARTADO. Cards archivadas.
2. **E-commerce/marketplace** (código ene 2026) — VIGENTE, alineado al Hub.
3. **Hub híbrido** (Drive feb 2026) — **MODELO OFICIAL CONFIRMADO** por Andrés (jul 2026).

Corrección clave: el portal de proveedores + flujo de aprobación de productos del código **SÍ sirven** (los usa el esquema de consignación). No descartarlos: completarlos.

---

## 3. Estado técnico

- **Stack:** ASP.NET Core MVC, EF Core, SQL Server, ASP.NET Core Identity, Bootstrap 5. Carpeta principal: `Web/`.
- **Repo:** `C:\Users\Usuario\Documents\GitHub\Bean-Sales` — **se movió FUERA de OneDrive (31-jul-2026)** porque la sincronización bloqueaba `.git` (`index.lock`/`HEAD.lock` constantes) y llegó a vaciar la carpeta vieja. Si aparece un `index.lock`, basta `Remove-Item .git\index.lock -Force`.
- **Ramas:** `dev` (trabajo) · `qa` (**publica en el sitio**: el deploy se dispara con push/merge a `qa`) · `master`. Todo el trabajo de julio 2026 está commiteado y desplegado en https://kingbean.runasp.net.
- **Hecho:** Fase 1 (seguridad, roles/permisos granulares vía atributo `HasPermission`, seed SuperAdmin). Fase 2 (catálogo `Product` con workflow de estados + aprobación, `Provider`, `ProductCountry` con stock por país). Fase 3 parcial (carrito híbrido invitado/usuario con fusión al login; checkout solo hasta vista de resumen).
- **Implementación del rediseño — Incremento 1 (jul 2026):** design system `Web/wwwroot/css/bean-theme.css` (tokens del handoff + componentes), layout de tienda `Web/Views/Shared/_LayoutStore.cshtml` (nav oscura + footer + fuentes Bitter/DM Sans), `_ViewStart` en `Home` y `Cart`, y storefront reestilizado: `Home/Index` (catálogo), `Home/Details`, `Cart/Index`, `Cart/Checkout`. Verificado por subagente: seguro para `dotnet build`, sin `.cs` tocados. Pendiente: build/commit por Andrés. Nota: `Home/Privacy` ahora hereda `_LayoutStore` (cosmético). Placeholders estáticos (rating, notas de cata, altura, proceso, lote, envío fijo) requieren campos nuevos en `Product` y el ciclo `Order` para wiring real.
- **Ciclo de venta — Incremento 2 (jul 2026):** modelos `Order`/`OrderItem` + enum `OrderStatus`, `DbSet`s y relaciones en `ApplicationDbContext` (Order→User y OrderItem→Product en `Restrict`, OrderItem→Order en `Cascade`, sin multiple cascade paths). `CartController`: POST `Checkout` (valida stock, crea orden con snapshot de precio, descuenta `Product.Stock`, vacía carrito, transacción) y GET `Confirmation`. Vista `Cart/Confirmation.cshtml` con línea de tiempo de seguimiento. Verificado por subagente: listo para migrar/compilar. **Andrés debe correr** (desde `Web/`): `dotnet ef migrations add AddOrders` → `dotnet ef database update` → `dotnet build`/`run`. Simplificaciones: envío fijo `18000`, pago sin pasarela (placeholder), stock global, país como string. **VALIDADO EN VIVO (22-jul-2026):** compra e2e OK — orden #DCF46AC3 creada, stock 36→34, carrito vaciado, checkout POST 200 sin errores. Único hallazgo: el bug de UI del "Total" sin envío (ver Alertas).
- **Ciclo BETA — Núcleo de datos (07-ago-2026, ⚠️ SIN COMPILAR, pendiente de validación de Andrés):** dos agentes entregaron E5.1 + fix de seguridad + E2 completa + E1 tanda A.
  - **E5.1:** 6 módulos de permiso nuevos (`Inventory`, `Warehouses`, `Pricing`, `Agreements`, `Settlements`, `Notifications`) en `Web/Constants/Modules.cs` y `ContextSeed.SeedPermissionsAsync`. Sin migración; **requiere reiniciar la app una vez**.
  - **🚨 Fix de seguridad:** `CompanyRolesController` POST `ManagePermissions` no revalidaba contra la allow-list (`// Relying on UI for now`) → un ProviderAdmin podía forjar GUIDs y auto-asignarse permisos de cualquier módulo. Ahora hay allow-list server-side compartida GET/POST + logging del intento + `asp-validation-summary` en la vista (antes el error ni se veía).
  - **E2 Pricing:** `Product` += `SupplierPrice`/`PriceSetAt`/`PriceSetBy`/`MarginAlert`; `PricingSettings` (seed **30 / 15 / 50**), `PriceChangeLog`, `OrderItem.SupplierPriceSnapshot`; `PricingCalculator` puro (22 tests) + `PricingService` + `PricingController` + 3 vistas + `_PricingFields`. Candado: no se activa un producto sin PVP fijado con `Pricing/Update`.
  - **E1 tanda A (sin UI):** `Warehouse`, `StockItem` (PK compuesta + `RowVersion`), `StockMovement` (libro mayor inmutable), `StockLedger` puro (18 tests) + `InventoryService` como ÚNICO punto de escritura de stock; `CartController`, `OrderManagementController` y `PaymentApplicationService` refactorizados.
  - **Migraciones `AddPricing` y `AddWarehouseInventory` ESCRITAS A MANO** (incluidos `.Designer.cs` y el snapshot) porque el sandbox no tiene .NET SDK. **Riesgo: correr `dotnet ef migrations list` ANTES de `database update`.**
  - **Decisión de diseño del agente a validar:** `Product.Stock` = `SUM(QuantityOnHand − QuantityReserved)`, no `SUM(QuantityOnHand)`, para no sobrevender durante los 45 min de reserva de Wompi. Documentado en el código con un bloque `NOTA DE DISEÑO`.
  - **Bug de negocio corregido de paso:** `SupplierOrdersController` calculaba "Tu pago" con `SubTotal` (PVP × cantidad) — con E2 le habría mostrado al proveedor el precio de venta de Bean con margen incluido. Ahora usa `SupplierPriceSnapshot × Quantity`.
  - **E1 tanda 3B — UI de inventario (07-ago-2026, ⚠️ SIN COMPILAR):** `InventoryController` (`/Inventory` matriz producto×bodega con tooltip en mano/reservado, 4 filtros + "solo stock bajo", totales por fila y columna, paginación 25, badge de reorden; `Details/{id}` con historial de movimientos paginado; `Adjust` con motivo obligatorio validado en servidor; `SetReorderPoint`) + `WarehousesController` (CRUD; bloquea desactivar bodega con stock > 0 y dar de baja la única del país; una sola `IsDefault` por país; valida DANE de 5 dígitos) + `ProductApproval/Receive` ahora pide bodega/cantidad/costo y escribe `Reception` + enlaces en `_Sidebar` + 6º KPI "Stock bajo" + **Mipaquete cotiza desde el `DaneCode` de la bodega que despacha** (fallback a `appsettings`). Sin migraciones. CSS sección 23.
    - **🔴 Trampa evitada — doble conteo:** `Reception` SUMA y la migración ya había creado los `StockItem`. Prellenar la cantidad con `Product.Stock` habría duplicado el inventario de todo lote migrado. Ahora solo se sugiere cantidad si el lote no tiene saldo; si ya lo tiene, avisa y permite recibir con 0. **Validar con un lote migrado real.**
    - Deuda: sin scoping multi-tenant en los 2 controladores nuevos (lo cierra E5.2); el filtro "solo stock bajo" y los totales por columna usan subconsultas con `Contains` (probar contra SQL Server real); `Ownership` de la recepción queda `BeanOwned` hasta E3; el sidebar ya hace 4 queries de conteo por request (consolidar, plan Free de 256 MB); sin tests nuevos.
  - **Deuda:** el reintento por `RowVersion` y la idempotencia de doble cancelación NO tienen test (falta proveedor EF real en `Tests.csproj`; **InMemory no sirve**, no soporta transacciones ni `rowversion`). Sin bodegas creadas, el checkout degrada al descuento directo sobre `Product.Stock` con `LogWarning`.
- **⚠️ Alertas abiertas:**
  - El "rediseño" de Andrés está **LOCAL SIN COMMITEAR** — recordarle hacer commit/push a `dev`.
  - Hay 4 logs de build borrados sin commitear en el working tree.
  - **Cobertura de tests mínima:** existen los primeros tests (`Tests/ShippingPackageBuilderTests.cs`, 10 casos; correr con `dotnet test Tests/Tests.csproj`). Falta cubrir los flujos críticos (checkout, aprobación, permisos).
  - **BUG (UI) del "Total" sin envío — CORREGIDO (22-jul, pendiente validar tras rebuild):** se centralizó `ShippingCost=18000m` en `CartController` (usado en las 4 rutas que renderizan Cart/Index|Checkout vía `ViewBag.Shipping`/`GrandTotal` y en `order.TotalAmount`). Vistas muestran Total con envío ($18.040) y botón "Pagar $18.040". Sin migración.
  - **BUG de aprobación — DIAGNOSTICADO Y CORREGIDO (22-jul-2026, pendiente validar tras rebuild):** al aprobar un producto salía **HTTP 405**. Causa: las acciones `Approve`/`Reject` no existían en `ProductApprovalController` (solo un comentario placeholder), pero la vista `ProductApproval/Index.cshtml` postea a ellas. Fix: se implementaron `[HttpPost] Approve` (PendingApproval→ApprovedToShip) y `[HttpPost] Reject` (→Rejected con motivo), con permiso `ProductApprovals/Update` y antiforgery. Sin cambios de modelo (no requiere migración). Falta: Andrés hace `dotnet build`/run y valida aprobar/rechazar. Observación menor: el `switch` de estados de `Index.cshtml` no muestra badge para `ApprovedToShip` (cosmético).
- **Enriquecer Product — Incremento 3 (jul 2026):** 10 campos nuevos en `Product` (Origin, Farm, Altitude, Process `CoffeeProcess?`, Variety, Lot, RoastDate, TastingNotes, Rating, ReviewCount — todos nullable) + enum `CoffeeProcess`. `ProductViewModel` + `ProductsController` (mapeos Create/Edit) y formularios `Products/Create|Edit` actualizados. Storefront (`Home/Index`, `Home/Details`) cableado a datos reales null-safe; se eliminaron los placeholders estáticos. Verificado por subagente: listo para migrar/compilar. **Andrés debe correr:** `dotnet ef migrations add EnrichProduct` → `dotnet ef database update`. Nota: `ContextSeed` NO siembra productos (los datos reales entran por el flujo del proveedor); el storefront degrada bien con nulls.
- **Mis pedidos — Incremento 4 (jul 2026):** `OrdersController` (`[Authorize] Index` lista los pedidos del usuario) + `Views/Orders/Index.cshtml` (historial estilo handoff vista 6: sidebar de cuenta, filtros píldora visuales, filas con estado; enlaza a `Cart/Confirmation/{id}` como detalle) + enlace "Mis pedidos" en `_LayoutStore` (solo autenticados) + estilos en `bean-theme.css`. Sin migración (usa el modelo `Order`). Verificado por subagente: compila. Placeholders: "Volver a pedir", Direcciones/Métodos de pago/Reseñas del sidebar.
- **Back-office — Incremento 5 (jul 2026):** shell del panel interno reestilizado al handoff (`_Layout.cshtml` + `_Sidebar.cshtml`: sidebar oscuro `#1d1510`, badge de rol, contador de pendientes vía `CountAsync`, topbar) + bandeja de **Aprobaciones** (`ProductApproval/Index` con tabs/badges) y `Receive` reestilizadas. Se arregló el switch de estados (ahora cubre los 6 `ProductStatus`, incl. `ApprovedToShip`/`Active`). Sin `.cs` ni migración; bindings Approve/Reject/Receive preservados. Verificado por subagente con `engineering:code-review`: compila. **Pendiente:** reestilizar el resto de vistas del panel (Products, Providers, Users, Roles, Company\*, + Dashboard, Matriz Café×País, vistas de proveedor) — hoy conviven con look SB-Admin viejo dentro del shell nuevo (mezcla visual, no rompe). Limpieza opcional: JS/CSS del sidebar viejo sin uso.
- **Lado proveedor — Incremento 6 (jul 2026):** `SupplierOrdersController` + `Views/SupplierOrders/Index.cshtml` + `SupplierOrderViewModel` — "Pedidos entrantes": el proveedor ve los pedidos que contienen sus productos, con **"Tu pago"** = suma de `SubTotal` de solo sus líneas (scoping por `ApplicationUser.ProviderID`, patrón de `ProductsController`, permiso `Products/Read`). `Products/Index` reestilizado al handoff vista 12 (stats reales vía `OrderItems.SumAsync`, badges de estado; bindings Create/Edit/SubmitForApproval/Ship preservados). Enlace "Pedidos entrantes" en el sidebar (solo proveedor). Sin `.cs` de modelo ni migración. Verificado por subagente con `engineering:code-review`: compila. Placeholder: CTAs "Marcar listo/Ver guía" son visuales (no hay transición de estado por-proveedor aún).
- **Páginas de acceso — Incremento 7 (jul 2026):** `_LayoutAuth.cshtml` split-screen del handoff + `_ViewStart` en `Views/Account/` (solo 4 vistas auth) → Login (vista 7), Register, RegisterProvider (vista 14 onboarding, radio-cards visuales sin campo real), RegistrationSuccess reestilizados. Bindings Identity preservados (`asp-for`/`asp-validation`/`returnUrl`/antiforgery). CSS scoped `.bean-auth-body`. Verificado (grep + `engineering:code-review`). Sin `.cs` ni migración.
- **Dashboard admin — Incremento 8 (jul 2026):** `DashboardController` (`[Authorize(Roles=SuperAdmin)]`) + `DashboardViewModel` + `Views/Dashboard/Index.cshtml` con KPIs REALES (ventas del mes vía `SumAsync`, pedidos, pendientes de aprobación, productos activos), gráfica "ventas por semana" (8 semanas, agrupada en memoria, barras CSS puras), lista "por aprobar" y "actividad reciente". Enlace "Dashboard" en el sidebar (solo SuperAdmin). NO cambia el landing (Home/Index sigue siendo el storefront). Verificado con `engineering:code-review`. Sin migración.
- **CRUD del panel — Incremento 9 (jul 2026):** las 17 vistas de Users, Roles, Providers, CompanyProfile, CompanyUsers, CompanyRoles reestilizadas al panel (clases `bean-panel-*`, `bean-btn-pill`, `bean-form-*`, `bean-perm-grid` en `bean-theme.css` sección 21) — se eliminó el look SB-Admin viejo. Bindings (`@model`, tag helpers, forms, antiforgery, checks de permeiso) preservados y verificados. Sin `.cs` ni migración. Con esto el back-office queda visualmente coherente (falta solo Matriz Café×País, diferida).
- **Acceso al panel desde la tienda — Incremento 11 (jul 2026):** diseño propuesto por agente y aprobado por PM (se difirió la redirección post-login y los placeholders del menú cliente). Implementado en `_LayoutStore.cshtml` (píldora **"Panel"** solo para staff → SuperAdmin a `Dashboard/Index`, Proveedor a `Products/Index`, con badge de pendientes; **menú desplegable de usuario** que reemplaza el trío suelto y ofrece accesos directos por rol y permiso), `bean-theme.css` (sección NAV + responsive) y `_Sidebar.cshtml` (enlace **"Ver tienda"**). Queries solo dentro de `if (isStaff)`. Sin `.cs` ni migración.
- **HTTPS — RESUELTO (27-jul-2026):** se emitió el certificado gratuito Let's Encrypt desde el panel de MonsterASP (renovación automática, vence 03-oct-2026). El sitio ya abre en móviles (antes daba `ERR_CONNECTION_RESET` porque Chrome fuerza `https://`). **Pendiente menor:** activar "Force redirect to HTTPS" en el panel para que quien entre por `http://` sea redirigido.
- **Gestión de pedidos del admin — Incremento 12 (jul 2026):** diseño propuesto por agente y aprobado por PM. `OrderManagementController` (bandeja `/OrderManagement` con tabs server-side + búsqueda + paginación 25, `Details`, `UpdateStatus`, `Cancel`) + `OrderWorkflow` (máquina de estados lineal Pending→Confirmed→Processing→Shipped→Delivered, sin retrocesos) + `OrderStatusUi` + ViewModels + 2 vistas. **Módulo de permisos nuevo `Orders`** (Read=ver, Update=avanzar, **Delete=cancelar**) sembrado vía `ContextSeed` (SuperAdmin lo recibe solo al arrancar; ProviderAdmin NO — usa `SupplierOrders`). **Regla de negocio clave: al cancelar se reintegra `Product.Stock`, salvo si el pedido ya estaba `Shipped`** (ya salió de bodega; se avisa al admin que ajuste a mano). Enlace "Pedidos" con badge en el sidebar; 4º KPI del Dashboard pasa a "Por despachar". Sin migración. Verificado con `engineering:code-review`: sin bloqueantes, sin `Guid.ToString()` ni métodos custom dentro de queries EF. **Requiere reiniciar la app una vez** para que el seed cree el módulo (si no, el enlace no aparece y da 403). Deuda anotada: dos `Cancel` simultáneos podrían reintegrar stock dos veces (no hay concurrency token); motivo de cancelación no se persiste (v1.1).
- **Navegación del storefront — Incremento 13 (jul 2026):** los 4 links de la nav eran placeholders (Orígenes y Mayoreo iban a `Home/Index`; Nosotros a la plantilla `Privacy` de .NET). Ahora: **Catálogo** → `Home/Index#lotes`, **Orígenes** → nueva `Home/Origenes` (agrupa productos activos por `Origin` con fincas/altura/lotes, datos reales, mismo criterio de visibilidad que `Index` incl. país por cookie), **Mayoreo** → `Account/RegisterProvider`, **Nosotros** → nueva `Home/Nosotros` (página de marca). `OrigenesViewModel` + 2 vistas + CSS sección 22 (aditivo) + link activo según ruta. `Privacy` se conserva para footer/legal. Verificado con `engineering:code-review`: sin bloqueantes; `GroupBy` en memoria tras `ToListAsync` (no rompe EF). Sin migración.
- **Envío dinámico — Tanda 1 (jul 2026):** base para cotizar envíos reales, **sin cambio visible** (sigue cobrando 18.000 vía el servicio nuevo). Entidad `ShippingCity` (catálogo DIVIPOLA) + `DbSet` + índice único en `DaneCode`; seed `SeedShippingCitiesAsync` desde `Web/Data/Seeds/Data/colombia-cities.json` (**169 municipios**, códigos DANE verificados); campos físicos en `Product` (`ShippingWeightGrams`, `LengthCm/WidthCm/HeightCm`) y de envío en `Order` (`ShippingCity`, `ShippingCityDaneCode`, `ShippingCarrier`, `ShippingEstimatedDays`, `ShippingQuoteSource`); `Web/Services/Shipping/` con `IShippingQuoteService` (+records), `FixedShippingQuoteService`, `ShippingPackageBuilder` (puro), `ShippingCityService` (caché); `CartController` refactorizado (fuera la constante 18000, helper `SetCartTotalsAsync` en las 5 rutas); formularios del proveedor con "Empaque y envío"; **primeros tests del proyecto** en `Tests/` (10 casos, proyecto fuera de la solución a propósito). Config en `appsettings.json` sección `Shipping` (`Provider: Fixed|Mipaquete`, defaults, origen, caché) — **sin secretos**. Migraciones: `AddShippingCityCatalog`, `AddDynamicShippingFields`, `SyncShippingSnapshot` (vacía a propósito).
- **Envío dinámico — Tanda 2 (jul 2026):** `MipaqueteShippingQuoteService` + checkout con **selector departamento→ciudad** (AJAX `Cart/Cities`, `Cart/QuoteShipping`), cotización en vivo (envío, total, botón y transportadora/días) y **re-cotización server-side en el POST** (el navegador solo manda el código DANE; el precio nunca viene del formulario). Persiste ciudad/DANE/transportadora/días/fuente en `Order`. No-Colombia → tarifa fija sin selector.
  - **Contrato de la API CONFIRMADO** (doc oficial leída por navegador, jul 2026): `POST {BaseUrl}/quoteShipping`, headers `apikey` + `session-tracker`; request `originLocationCode`/`destinyLocationCode` (**DANE de 8 dígitos** = 5 + `000`), `height`/`width`/`length`/`weight` **enteros**, `quantity`, `declaredValue`; respuesta = **array** con `shippingCost`, `deliveryCompanyName`, `deliveryCompanyId`, `score` y **`shippingTime` en MINUTOS** (÷1440 para días). **Array vacío = sin cobertura** (bloquea). Base URL de pruebas: `https://api-v2.dev.mpr.mipaquete.com` (configurable sin recompilar).
  - Fallos: timeout/error → tarifa fija (`Fallback`, nunca bloquea la venta); sin cobertura → bloquea con mensaje. Caché por ruta+peso+valor (`QuoteCacheMinutes`); precios redondeados hacia arriba a múltiplos de 50 COP.
  - **Origen configurado:** Cali, DANE **76001** (ojo: 760001 es el código POSTAL, no el DANE). El código concatena `000` → `76001000`.
  - **✅ DESBLOQUEADO (31-jul-2026) — contrato REAL descubierto y validado en vivo.** La documentación pública (Postman) describe un entorno DEV que **no es** la API que usa el panel. Se descubrió inspeccionando el tráfico de `app.mipaquete.com` (DevTools → Network → `quoteShipping`). Contrato real:
    - **`POST https://core.mipaquete.com/routes/quoteShipping`** (no `api-v2.*.mipaquete.com`, que ni siquiera resuelve por DNS).
    - **Auth: header `customer-key`** = UUID del comercio (`74390816-…`), **NO** el JWT `apikey`. También viaja `session-tracker`.
    - Payload: `originCountryCode`/`destinyCountryCode` = **"170"** (Colombia), `originLocationCode`/`destinyLocationCode` (DANE 5 + `000`), `height`/`width`/`length`/`weight` enteros, `quantity`, `declaredValue`, `saleValue` (0 si no es contraentrega).
    - Respuesta: array con `shippingCost`, `deliveryCompanyName`, `deliveryCompanyId`, `deliveryCompanyImgUrl`, `score`, **`shippingTime` en MINUTOS**, `routeType`, `insuranceValue`… **Prueba real** (Cali 76001000 → Bogotá 11001000, 10×10×10 cm, 3 kg, declarado 10.000): SERVIENTREGA 20.650 · ENVIA 20.950 · COORDINADORA 23.280 · TCC 25.800.
    - **Código ya ajustado** al contrato real; `Program.cs` ahora activa el proveedor según `CustomerKey` (antes miraba `ApiKey`, lo que habría dejado tarifa fija en silencio). Nuevo secret: **`MIPAQUETE_CUSTOMER_KEY`**; `MIPAQUETE_API_KEY` quedó sin uso en cotización.
    - El ticket con soporte (ID 6a68feb29c5991a1212a81ee) queda **sin necesidad de respuesta**; el JWT de la cuenta no servía porque ese endpoint no lo usa.
  - Pendiente v1.1: generar guía (`createSending`), recogida y tracking en "Mis pedidos"; pago contraentrega.
- **⚠️ Incidente (28-jul-2026) — lección aprendida:** se ejecutó `git filter-repo` (para purgar del historial la contraseña de BD que estaba en `appsettings.json`) **con cambios sin commitear**; la herramienta hace reset y **se perdieron todas las modificaciones a archivos tracked** de la tanda 1 (los archivos nuevos sobrevivieron). Se rehicieron usando las migraciones como especificación. **Regla: SIEMPRE commitear antes de cualquier reescritura de historial.** Además, OneDrive bloquea `.git` y provoca `index.lock`/`HEAD.lock`: pausar la sincronización antes de operaciones git pesadas. Nota: la contraseña de la BD estuvo commiteada — **debe rotarse** en el panel de MonsterASP.
- **Pagos — Wompi (jul 2026, en curso):** pasarela elegida (Bancolombia). Cuenta creada como **persona natural** (Andrés, C.C.), sin SAS todavía; RUT pendiente (se sube después, no bloquea el sandbox). Sector CIIU: comercio al por menor. **Tarifa de pagos en línea: 2,65% + $700 + IVA por transacción exitosa** — incluye tarjetas, PSE, Nequi, Daviplata, QR/Botón Bancolombia, BNPL, corresponsal. Detalle e impacto por ticket en **`Docs/costos_pasarela_wompi.md`** (regla práctica: provisionar ~4% del total cobrado; el fijo de $700 castiga tickets pequeños). Requisito del registro: 2 redes sociales activas del negocio (pendiente crearlas — también es el primer paso de GTM). **Diseño APROBADO e IMPLEMENTADO (28-jul):** Web Checkout (redirección) + orden en `Pending` con **reserva de stock** + confirmación por **webhook** `POST /pagos/wompi/eventos` (valida firma con `EventsSecret`, verifica monto, idempotente, solo transiciona desde `Pending`).
  - Nuevos: `Models/Enums/PaymentStatus.cs`, `Services/Payments/` (`WompiOptions`, `WompiSignature` puro + tests con vectores de la doc, `IPaymentGateway`, `WompiPaymentGateway`, `PaymentApplicationService`, `PendingOrderExpirationService`), `Controllers/PaymentWebhookController.cs`, `Views/Cart/PayRedirect.cshtml`. `Order` +10 campos de pago. Sección `Wompi` vacía en `appsettings.json`; secretos solo en Development/Production (gitignored) y GitHub Secrets (`WOMPI_*`, ya inyectados por el pipeline de forma defensiva).
  - **Regla de stock:** aprobado → `Confirmed` (stock ya reservado); rechazado → sigue `Pending` con stock reservado y opción de reintentar/cancelar; anulado o cancelado → reintegra; abandonado → expira a los **45 min** (barrido perezoso que reconcilia contra Wompi antes de liberar).
  - **Degradación segura:** si no hay llaves cargadas (`CanCharge == false`) el checkout se comporta como antes (confirma sin cobrar), así que se puede desplegar sin romper el sitio.
  - Dashboard, actividad reciente y "Pedidos entrantes" del proveedor ahora filtran por `PaymentStatus == Approved`; el admin ya no puede avanzar a mano un pedido no pagado.
  - **Pendiente de Andrés:** `dotnet ef migrations add AddWompiPayments` **+ backfill** (`UPDATE [Orders] SET [PaymentStatus]=1, [PaidAt]=[OrderDate] WHERE [OrderStatus] <> 5;`) antes de `database update`; pegar `IntegritySecret` y `EventsSecret` (hoy placeholders); registrar la URL de eventos en el panel de Wompi; y **activar "Force redirect to HTTPS"** (con pagos ya es bloqueante).
  - Sin confirmar (verificar en sandbox): header de auth de `GET /transactions/{id}` (se intenta privada → pública) y el formato de `expiration-time`. Deuda: sin `RowVersion` (concurrencia) y sin entidad `PaymentTransaction` (histórico de intentos).
- **Galería de fotos de producto — Cloudinary (jul 2026):** antes `Product.ImageUrl` era un campo suelto que **nunca se llenaba** (el form no lo exponía) y `Home/Details` tenía 3 thumbnails decorativos vacíos. Ahora: entidad **`ProductImage`** (1:N, con `PublicId`, orden, portada, alt, quién subió) + `Product.ImageUrl` **sincronizado como portada denormalizada** (no rompe las 6 vistas que ya lo usaban: catálogo, carrito, checkout, confirmación, Mis pedidos, aprobaciones). **Subida directa navegador → Cloudinary** firmada por el backend (el archivo nunca pasa por el hosting de 256 MB); `Web/Services/Media/` con `CloudinarySignature` (pura, 26 tests, algoritmo verificado contra el SDK oficial: SHA-1 de params ordenados + api_secret), `CloudinaryImageStorage`, `DisabledImageStorage` (degradación), `ProductImageService`; `ProductImagesController` (ticket/registrar/portada/reordenar/alt/eliminar, con antiforgery y scoping); partial `_ProductImageManager` + `wwwroot/js/bean-media.js`; galería real en `Home/Details` (principal + miniaturas clicables) y portada con transformación en el catálogo. **Permisos reutilizados** (`Products/Update` para el proveedor solo en Draft/Rejected; `ProductApprovals/Update` para el admin en cualquier estado, incl. dentro de `Receive` para fotografiar el lote al recibirlo) — sin módulo nuevo, no requiere reiniciar para sembrar. Límites validados en cliente y servidor: jpg/png/webp, 5 MB, 6 fotos por producto, mínimo 800×800. **Create redirige a Edit** tras guardar (el producto debe existir antes de colgarle fotos). Config: sección `Cloudinary` vacía en `appsettings.json` + secrets `CLOUDINARY_CLOUD_NAME/API_KEY/API_SECRET` inyectados de forma defensiva en el pipeline. **Andrés debe:** crear cuenta gratis en cloudinary.com, copiar las 3 credenciales a `appsettings.Development.json` y a GitHub Secrets, **NO crear preset unsigned**, y correr `dotnet ef migrations add AddProductImages` → `database update`. Deuda: imágenes huérfanas si el navegador muere entre la subida y el registro (limpieza en fase 2).
- **Conexión a BD (jul 2026):** la BD de **desarrollo/pruebas es la de MonsterASP** (`db60805`, SQL Server remoto); la **localdb queda descartada**. La cadena vive en `Web/appsettings.Development.json` (dev) y `Web/appsettings.Production.json` (host), **ambos gitignored y untracked** — la contraseña NO se sube al repo. `dotnet run` (entorno Development por defecto) apunta a MonsterASP y auto-migra al iniciar. `appsettings.json` conserva la cadena localdb como base no usada. En el sitio desplegado, la cadena va en el panel de MonsterASP como `DefaultConnection`.
- **Despliegue — CI/CD (jul 2026):** MonsterASP.NET + GitHub Actions. **Sitio: `site81000` → https://kingbean.runasp.net** (plan Free, .NET x86, 256 MB RAM). **HTTPS activo** (Let's Encrypt gratuito emitido 27-jul-2026, renovación automática; pendiente activar "Force redirect to HTTPS" en el panel).
  - **Flujo de ramas (ajustado por Andrés, 28-jul-2026): el deploy se dispara con push a la rama `qa`** (antes `dev`/`master`), es decir al mergear un PR hacia `qa`. `dev` es la rama de trabajo. ⚠️ La rama `qa` debe existir en el remoto (`git checkout -b qa && git push -u origin qa`).
  - Workflow `.github/workflows/deploy.yml`: `windows-latest`; publica **`--runtime win-x86 --self-contained false`** (app pool de 32 bits); **inyecta secretos en el `appsettings.json` publicado** (connection string siempre; API key de Mipaquete y origen de envío solo si los secrets existen — inyección defensiva); despliega con `rasmusbuchholdt/simply-web-deploy@2.2.0`. `Program.cs` auto-migra y siembra al iniciar.
  - **GitHub Secrets:** `MONSTERASP_WEBSITE`=site81000, `MONSTERASP_SERVER`=https://site81000.siteasp.net:8172, `MONSTERASP_USERNAME`=site81000, `MONSTERASP_PASSWORD`=(WebDeploy), `MONSTERASP_CONNECTION_STRING`; y para envío (opcionales): `MIPAQUETE_API_KEY`, `SHIPPING_ORIGIN_DANE`, `SHIPPING_ORIGIN_CITY`.
  - Guía: `Docs/deploy_monsterasp.md`. **Riesgo clave:** las migraciones EF deben estar commiteadas o el host no las aplicará.
- **Bloqueantes para vender de verdad:** el ciclo de venta (`Order`/`OrderItem`) y el `Product` enriquecido ya están commiteados y funcionando en producción. Falta: **pasarela de pago real** (Wompi/PSE COP — hoy el checkout NO cobra) y **terminar el envío dinámico** (tanda 2 con Mipaquete; hoy tarifa fija 18000).

---

## 4. Dónde está la información

- **🔴 EVIDENCIA DE MERCADO — LEER ANTES DE CUALQUIER DECISIÓN DE PRODUCTO (7-ago-2026):** `Docs/correccion_dossier_viabilidad_2026-08.md` — corrige el dossier de los 4 críticos con datos verificados. Tres hallazgos: (1) el portal de la FNC solo hace **~18.900 visitas/mes** comprando el 56%, o sea ~340 visitas por marca — el B2C doméstico sigue NO VIABLE pero porque **la categoría no tiene demanda orgánica**, no porque el espacio esté ocupado; (2) **la Ruta B (exportación/MoR) ya la opera la FNC con Almacafé desde sep-2024** (tostado en origen a EE.UU. en <72 h, sin inventario en destino, vía Amazon/Walmart) y el registro de exportador es **gratis y sin intermediarios** — el "foso regulatorio" no existe; (3) la Ruta A recosteada da **3,7%–16,8%** (no 7,6%) y necesita **~$43M COP** de capital, con el agravante de que el tostador ya le vende a la cafetería a **23,5% off directo** — el proveedor es también el competidor. **Ninguna de las 2 rutas de salvamento del dossier sobrevive intacta.** Guion corregido de las 15 llamadas en la §7 de ese doc.
- **⭐ Requerimientos del ciclo BETA (agosto 2026):** `Docs/epicas_beta_2026-08.md` — 5 épicas (inventario por bodegas, pricing/markup, acuerdos+consignación, notificaciones, permisos) con modelo de datos, historias de usuario, criterios de aceptación, orden de ejecución y riesgos.
- **Briefs de handoff a agentes de desarrollo:** `Docs/handoff_agentes_beta_2026-08.md` — un prompt listo por agente, con reglas comunes y secuencia de dependencias.
- **Plan de trabajo vigente:** `Docs/plan_de_trabajo_hub_2026-07.md` (fuente de verdad del roadmap).
- **Diagnóstico anterior (obsoleto en parte):** `Docs/project_status_and_roadmap.md` (8-jul-2026, decía "compra-reventa pura"; corregido por el plan de julio).
- **Specs originales:** `Docs/design_specification.md`, `Docs/implementation_plan.md`.
- **Google Drive — carpeta `Coffee Global Hub`** (conector Drive conectado):
  - `Plan de negocios/PLAN DE NEGOCIO.docx` — modelo de negocio completo.
  - `Plan de negocios/Exportación de Café Colombia a Canadá.docx` — legal/logística/costos (~USD 26-28/kg).
  - `Flujos de comportamiento/` — 4 diagramas PNG: registro/validación de proveedor, creación/aprobación de productos, abastecimiento a bodega, comportamiento de cliente.
- **Trello — tablero "The kng bean Fase 1"** (conector Trello conectado):
  URL: https://trello.com/b/S30tmyS3/the-kng-bean-fase-1
  Listas clave creadas jul 2026: `✅ Logros alcanzados`, `🔧 Cierre Técnico – Fase 2`, `🌐 Negocio: Legal/Operaciones/Finanzas/GTM`.
- **⭐ Rediseño aprobado (FUENTE DE VERDAD de UI):** `Docs/design_handoff_bean_redesign/` — handoff hi-fi con `README.md` (design system completo + specs de 15 vistas), `screenshots/01–15` y prototipos `.dc.html`. Dirección visual "4a moderna animada": base café oscuro `#1d1510`, acento terracota gradiente `linear-gradient(135deg,#d98a5f,#B4552E)`, tipografías **Bitter** (titulares) + **DM Sans** (cuerpo), píldoras y motion sutil. Ya contempla el modelo Hub (selector Colombia/Canadá, matriz Café×País, precios COP/CAD, aprobación con comentarios). **Recrear pixel-perfect en las vistas Razor.**
- **Figma** (conector Figma conectado, cuenta de Andrés, plan Starter):
  - Diagramas de flujo (FigJam): https://www.figma.com/board/oCNE7wyX7khhsLGQoWJa65 ✅ útil.
  - UI reconstruida del Bootstrap del código: https://www.figma.com/design/weaMwgHnOu1k1CaPCKmy4L — **SUPERADA por el handoff aprobado**. No usar como referencia de diseño; el handoff manda.
  - **Figma DESCARTADO (jul 2026):** el plan Starter tiene un tope de llamadas MCP que exige upgrade; no es viable recrear el diseño ahí. El handoff en `Docs/` es la fuente de diseño para implementar en código. Retomar solo si Andrés sube de plan.

---

## 5. Roadmap resumido (detalle en el plan de julio)

- **P0 (decisiones de PO):** confirmar mercado piloto (¿Canadá?); capital de trabajo disponible; si arranca con compra directa/consignación/ambos.
- **P1 (cierre técnico – Fase 2):** commitear rediseño; `Order`/`OrderItem` + persistencia + descuento de stock; pagos reales (Wompi/PSE COP · Stripe/PayPal USD); país/moneda por IP; cálculo de envío + consolidación; completar panel de proveedor + filtro de admisión (RUT, Cámara, Invima, FNC + muestra física); tests.
- **P2 (negocio en paralelo):** SAS Colombia + registro exportador; estructura/importación en destino (SFC/CFIA, etiquetado bilingüe); bodega + alianzas de logística; modelo financiero; primer proveedor y primeros clientes.

---

## 6. Decisiones de PO

**Tomadas (21-jul-2026):**
- **Mercado de arranque: Colombia (local primero).** Sin exportación en el lanzamiento inicial → NO se necesita SFC/CFIA/etiquetado Canadá al inicio. **La exportación a Canadá pasa a Fase 2.** Foco inicial: SAS Colombia, logística nacional, pagos COP (Wompi/PSE).
- **Modelo de lanzamiento: AMBOS a la vez** (compra directa + consignación).
- **Branding: identidad propia YA aprobada** en el handoff (dirección "4a moderna animada"). No re-abrir.

**Tomadas (07-ago-2026) — ciclo BETA. Detalle en `Docs/epicas_beta_2026-08.md`:**
- **Relación con proveedor: HÍBRIDO POR PROVEEDOR**, y **los tres esquemas completos en la beta** (consignación, compra en firme, comisión). Cada proveedor se configura con su `ProviderAgreement`.
- **Precio de venta: PVP MANUAL por SKU.** Se separa `SupplierPrice` (costo, lo pone el proveedor) de `Price` (PVP, lo pone Bean). Con margen en vivo, precio sugerido según % objetivo y **alerta** cuando el costo del proveedor rompe el margen mínimo. Nada de recálculo automático. Candado: un producto no pasa a `Active` sin PVP fijado.
- **Bodegas propias de Bean.** Hoy 1 (Cali) pero el modelo es **multi-bodega y multi-país desde el inicio**. `Product.Stock` pasa a ser un total denormalizado; la verdad es el libro de `StockMovement`. Traslados entre bodegas → v1.1.
- **Margen: objetivo 30% / mínimo 15%.** El 15% es el umbral de la ALERTA, no la meta: con 15% y Wompi (2,65% + $700 + IVA) el neto real cae a ~10% antes de bodega y logística.
- **Liquidación al proveedor: se dispara con la ENTREGA** (`OrderStatus == Delivered` + `PaymentStatus == Approved`), con **corte quincenal** por defecto (configurable por proveedor). Bean no paga café perdido en tránsito ni devuelto.
- **Aviso al proveedor: INFORMATIVO** (correo + panel). Se le informa el PVP aplicado sobre su precio; no puede bloquearlo. Texto aprobado en el doc de épicas.
- **Permisos:** el sistema **ya es dinámico** (auditado). `ApplicationRole` es entidad de BD con `ProviderID`, `ApplicationUser` tiene `ProviderID`, y `Permission → ParametricPermission → ParametricModule` se evalúa en runtime con `[HasPermission]`. `CompanyRoles`/`CompanyUsers` ya permiten que un proveedor gestione sus propios roles y usuarios. **No rehacer.** Gaps: sembrar módulos nuevos y blindar el aislamiento multi-tenant con tests.

**Aún pendientes:**
- Capital de trabajo para el primer inventario: **sin definir** → trabajar en el modelo financiero (con "ambos", la consignación reduce la necesidad de capital).
- ¿Ya hay proveedor candidato con quien se haya hablado?
- **¿La comisión se calcula sobre el PVP o sobre el neto tras Wompi** (2,65% + $700 + IVA)?
- **¿Quién asume pasarela y envío en la liquidación de consignación?**
- **¿Qué tipo de acuerdo se asigna por defecto a los proveedores ya existentes?** (bloquea la migración de E3)

---

## 7. Convenciones de trabajo

- Actuar como PM: mantener Trello como fuente de verdad, documentar decisiones, hacer preguntas de PO antes de avanzar sobre supuestos.
- Al cerrar trabajo relevante, actualizar este `CLAUDE.md` y el plan en `Docs/`.
- Conectores disponibles: Trello, Google Drive. (GitHub/Slack/otros requieren autorización del usuario si se necesitan.)
- **⚠️ Razor gotcha (RZ1010):** dentro de un bloque de código (`@foreach`/`@for`/`@if` `{ }`) ya se está en contexto C#; escribir `@{ ... }` ahí falla al compilar (RZ1010). Usar sentencias sueltas (`var x = ...;`) sin `@{`. Los subagentes de QA NO lo detectan (requiere `dotnet build` real). Corregido en ProductApproval/Index y Receive (jul 2026).
- **Subagentes usan skills del plugin `engineering`:** QA/verificación con `engineering:code-review`; desarrollo/diagnóstico con `engineering:debug`, `engineering:architecture`, `engineering:testing-strategy` según aplique. (Instrucción de Andrés, jul 2026.)
- Credenciales SuperAdmin de la app y detalles de instalación: ver `README.md`.
